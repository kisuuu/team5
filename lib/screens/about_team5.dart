import 'package:flutter/material.dart';

class AboutTeam5Screen extends StatelessWidget {
  const AboutTeam5Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        appBar: AppBar(
          title: const Text('About'),
        ),
      ),
    );
  }
}
